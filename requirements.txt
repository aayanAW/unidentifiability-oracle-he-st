# Unidentifiability Oracle (H&E->ST) -- runtime dependencies.
#
# IMPORTANT: install torch FIRST matching your cluster's CUDA, e.g.
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# then `pip install -r requirements.txt`. The >=2.0 floor is what src/train.py needs (AMP autocast +
# torchrun/DDP API). Local dev was torch 2.0.0 (CPU/MPS); the cluster wants a matching-CUDA build.
torch>=2.0
timm>=1.0            # ungated DINOv2-S backbone: vit_small_patch14_dinov2.lvd142m (end-to-end mode)
numpy>=1.23,<2.0     # <2.0: scanpy 1.10 / the pinned stack
scipy>=1.10          # Clopper-Pearson (conformal CIs), permutation tests
scikit-learn>=1.3    # RF / KNN substrates, AUROC
scanpy>=1.10         # 10x cell_feature_matrix.h5 readers
pandas>=2.0          # cells.parquet / homography / Visium positions
tifffile>=2024.1     # lazy OME-TIFF zarr window reader (memory-safe H&E patching)
zarr>=2.16,<3        # zarr<3: the lazy window store API used by embeddings._read_windows_lazy
pyyaml>=6.0          # configs/train_breast.yaml
matplotlib>=3.7      # experiments/make_figures.py
